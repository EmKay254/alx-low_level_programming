#include "main.h"

/**
 * print_numbers - prints numbers 1 through 9
 * Return: Always success
 */

void print_numbers(void)
{
	char i;

	for (i = '0'; i <= '9'; i++)
	{
		_putchar(i);
	}
	return (0);
}
