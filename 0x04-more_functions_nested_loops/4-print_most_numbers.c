#include "main.h"

/**
 * print_most_numbers - prints numbers between 0 to 9
 * excluding 2 and 4
 * Return: 0
 */

void print_most_numbers(void)
{
	int num == 48;

	for (num = 48; num <= 57; num++)
	{
		if ((num == 50)) || ((num == 52))
		continue;
	}
	_putchar(num);
}
