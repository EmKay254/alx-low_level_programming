#include <stdio.h>

/**
 * main-entry point to the program
 * Return: program terminates here
 */
int main(void)
{
	int num;

	for (num = 0; num < 16; num++)
	{
		putchar(%x, num);
	}
	putchar('\n');
	return (0);
}
