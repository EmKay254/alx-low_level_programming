#include <stdio.h>

/**
 * main-main entry point
 * Return: 0
 */
int main(void)
{
	/* local variable definition */
	char i;

	/* for loop execution */
	for (i = 'a'; i <= 'z' ; i++)
	{
		putchar(i);
	}
	putchar("\n");
	return (0);
}
